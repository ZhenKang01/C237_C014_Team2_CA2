<!DOCTYPE html>
<html lang='en'>
<head><meta charset='UTF-8'><meta name='viewport' content='width=device-width, initial-scale=1.0'><title>Student dashboard - TutorLink</title><link rel='stylesheet' href='/css/app.css'></head>
<body class='dashboard-page'>
    <nav class='nav shell'><a class='brand' href='/'><span class='brand-mark'>T</span>TutorLink</a><div class='user-nav'><b class='role-pill student-pill'>Student</b><span><strong><%= user.full_name %></strong><small><%= user.email %></small></span><a class='button secondary' href='/profile'>Profile</a><a class='button secondary' href='/logout'>Log out</a></div></nav>
    <main class='dashboard shell with-sidebar'>
        <aside class='sidebar'>
            <nav class='sidebar-nav'>
                <a href='/dashboard' class='sidebar-link active'>
                    <span class='icon'>🏠</span>
                    Dashboard
                </a>
                <a href='/student/slots' class='sidebar-link'>
                    <span class='icon'>🔍</span>
                    Find Tutors
                </a>
                <a href='/student/my-bookings' class='sidebar-link'>
                    <span class='icon'>📅</span>
                    My Bookings
                </a>
            </nav>
        </aside>
        <div class='dashboard-content'>
            <% if (errorMessages.length) { %><div class='alert error'><%= errorMessages.join(' ') %></div><% } %>
            <% if (successMessages.length) { %><div class='alert success'><%= successMessages.join(' ') %></div><% } %>
            <header class='dashboard-header'><div><span class='eyebrow'>Student workspace</span><h1>Let's keep moving, <span class="capitalize"><%= user.full_name.split(' ')[0] %></span>.</h1><p>Your learning support, upcoming activity, and progress start here.</p></div><div class='profile-mark student-avatar'>ST</div></header>
            
            <section class='stat-grid' style='margin-bottom: 32px;'>
                <article><strong><%= totalBookings %></strong><small>Total Bookings</small></article>
                <article><strong><%= pendingApprovals %></strong><small>Pending Approvals</small></article>
                <article>
                    <% if (nextSession) { %>
                        <strong><%= new Date(nextSession.slot_date).toLocaleDateString('en-SG', { day: 'numeric', month: 'short', year: 'numeric' }) %></strong>
                        <small><%= nextSession.slot_time %></small>
                    <% } else { %>
                        <strong>None</strong>
                        <small>Next Session</small>
                    <% } %>
                </article>
            </section>
        </div>
    </main>
</body>
</html>
